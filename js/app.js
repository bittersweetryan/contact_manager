/*
app.js holds defaults and common utility functions across the
application, and helps namespace all of the app code.  In this 
case it'll be quite small.
*/

window.InContact = {
	ajaxDefaults: {
		cache: false,
		timeout: 5000,
		type: 'post'
	}
};